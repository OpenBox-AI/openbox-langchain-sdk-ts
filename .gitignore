# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Compiled output — recipients run `npm install && npm run build` themselves
dist/

# Lock file (optional — include if you want reproducible installs)
# package-lock.json

# OS / editor noise
.DS_Store
Thumbs.db
*.log
